/* eslint-disable import/prefer-default-export */
import { COLOR } from "./color"

export const config = {
  defaultIconType: "MaterialCommunityIcons",
  defaultIconSize: 20,
  defaultIconColor: COLOR.BLACK,
};
